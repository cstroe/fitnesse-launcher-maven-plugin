#summary An example SQL Project
#labels Featured

== An example SQL Project ==

=== A project configuration (pom.xml, etc) for using fitnesse-launcher-maven-plugin to execute `FitNesse` integration tests for a simple SQL project ===

  # [http://code.google.com/p/fitnesse-launcher-maven-plugin/downloads/detail?name=fitnesse-launcher-sql-example-1.2.0-src.zip Download the example SQL project]
  # Run `FitNesse` as a wiki server using:
{{{
mvn clean install -P wiki
}}}
  # Visit `http://localhost:9123/SqlExample` and explore!
  # Run `FitNesse` as automated integration tests using:
{{{
mvn clean install -P auto
}}}
  # The JUnit XML results can be found at target/fitnesse/results
  # The `FitNesse` HTML reports can be found at target/fitnesse/reports